
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
char low;
for (char low = a;char low <= z; low++)
     putchar(low);
for (char low = A; char low <= Z; low++)
     putchar(low);
putchar(n);
 
    return (0);
}
